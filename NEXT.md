DONE: The hard-concept sequencing build is complete (four authored pages, exposure metrics rejected, two wrong spec spans corrected), plus two owner-reported UI fixes: Return-to-Reading now aligns with the Study sidebar's left content edge, and the sidebar's bottom padding grew so Settings and that button are visible together at maximum scroll.
NEXT: Owner input needed on what comes after content. Remaining scoped-but-unbuilt work is section 8.5 (securitization flagship depth), section 9's learning-coherence build (planner spine, ReadingArc, visual builders), and section 7.3 (device licensing, needs a backend).
BLOCKERS: The two CSS fixes were verified in the compiled bundle and by marker-clean render-checks, but not by a screenshot: the button needs seeded localStorage that the headless harness could not load. Worth a 10-second look in a real browser.
