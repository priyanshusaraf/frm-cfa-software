DONE: Moved the fullscreen Menu handle from top-centre to top-left, and made it drop below the nav (by a measured --peek-h, not a hardcoded offset) once the nav is pinned open, so it never covers the brand.
NEXT: Curate premium replacement sources for the 21 readings now left with zero (regulators, standard-setters, original papers) as part of the content run.
BLOCKERS: none.
